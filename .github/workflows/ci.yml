name: CI/CD Pipeline

on:
  push:
    branches: [ "main" ]
    tags: [ "v*" ]
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set up Go
      uses: actions/setup-go@v5
      with:
        go-version-file: go.mod
        cache: true

    - name: Install System Dependencies
      env:
        DEBIAN_FRONTEND: noninteractive
      run: |
        sudo apt-get update -qq
        sudo apt-get install -yq libgl1-mesa-dev xorg-dev libxcursor-dev libxi-dev libxinerama-dev libxrandr-dev libxxf86vm-dev libwayland-dev libxkbcommon-dev desktop-file-utils

    - name: Verify Code Formatting
      run: |
        if [ "$(gofmt -s -l . | wc -l)" -gt 0 ]; then
          echo "The following files are not formatted correctly:"
          gofmt -s -l .
          exit 1
        fi

    - name: Run go vet
      run: go vet ./...

    - name: Run Tests
      run: go test -v -race ./...

    - name: Build Application
      run: go build -tags wayland -trimpath -ldflags="-s -w" -o bin/solus-welcome ./src

    - name: Test Staged Installation
      run: |
        chmod +x test/install_test.sh
        ./test/install_test.sh

    - name: Upload Build Artifact
      uses: actions/upload-artifact@v4
      with:
        name: solus-welcome
        path: bin/solus-welcome

  release:
    if: startsWith(github.ref, 'refs/tags/v')
    needs: build-and-test
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
    - uses: actions/checkout@v4

    - name: Download Build Artifact
      uses: actions/download-artifact@v4
      with:
        name: solus-welcome
        path: release-bin

    - name: Install Release Dependency
      run: |
        sudo apt-get update -qq
        sudo apt-get install -yq upx
        upx --version

    - name: Package Release Assets
      run: |
        upx --best --lzma release-bin/solus-welcome
        ARCHIVE_NAME="solus-welcome-${{ github.ref_name }}-linux-x86_64.tar.gz"
        PACKAGE_DIR_NAME="solus-welcome-${{ github.ref_name }}"
        STAGING_DIR="$(mktemp -d)"
        PACKAGE_DIR="$STAGING_DIR/$PACKAGE_DIR_NAME"
        mkdir -p "$PACKAGE_DIR/bin"
        cp release-bin/solus-welcome "$PACKAGE_DIR/bin/"
        cp -r assets solus-welcome.desktop install.sh README.md "$PACKAGE_DIR/"
        tar -czf "$ARCHIVE_NAME" -C "$STAGING_DIR" "$PACKAGE_DIR_NAME"
        for required_path in bin/solus-welcome assets/config.toml install.sh README.md solus-welcome.desktop; do
          required_path="$PACKAGE_DIR_NAME/$required_path"
          tar -tzf "$ARCHIVE_NAME" "$required_path" >/dev/null
        done
        for excluded_path in src go.mod go.sum; do
          excluded_path="$PACKAGE_DIR_NAME/$excluded_path"
          if tar -tzf "$ARCHIVE_NAME" "$excluded_path" >/dev/null 2>&1; then
            echo "Unexpected source path in release archive: $excluded_path" >&2
            exit 1
          fi
        done
        rm -rf "$STAGING_DIR"
        sha256sum "$ARCHIVE_NAME" > checksums.txt

    - name: Create GitHub Release
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      run: |
        gh release create "${{ github.ref_name }}" \
          "solus-welcome-${{ github.ref_name }}-linux-x86_64.tar.gz" \
          "checksums.txt" \
          --title "Solus Welcome ${{ github.ref_name }}" \
          --generate-notes
